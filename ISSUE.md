# Issues & Backlog

## 🔍 UIUX 待辦

### 操作流程
- [ ] 工具列分層重組：文件操作 / 編輯工具 / 輸出 三區分離
- [ ] 載入單一檔案後直接進入標示或文字模式，而非頁面總覽
- [ ] 文字框操作邏輯：點擊內部 = 編輯，點擊邊框 = 移動/縮放

### 視覺設計
- [ ] 圖示統一尚未完成：縮圖側欄旋轉/刪除（↺↻✕）、合併清單移除（✕）、拆分移除（✕）仍用符號字元

### 互動回饋
- [ ] Hover 預覽：游標移至可互動物件時顯示高亮或虛線外框
- [ ] Undo / Redo 機制（繪圖、刪除頁面等破壞性操作）
- [ ] 錯誤 Toast 改為需手動關閉的 Modal

### 無障礙設計（a11y）
- [ ] 自訂下拉選單加鍵盤支援（Enter / 方向鍵 / Esc）
- [ ] 顏色選擇器加 Tooltip 或打勾圖示，改善色弱使用者體驗

---

## 🔐 規劃中功能

### PDF 後端加密（推薦方案）

- **方案**：前端導出 PDF → 送後端加密 → 用戶下載加密檔案
- **優點**：
  - 使用成熟的 PDF 庫（pypdf、reportlab 等）
  - 原生支援 PDF 安全處理程序（RC4、AES）
  - 可設定用戶密碼 / 所有者密碼 / 權限控制
  - 實作簡單（~5 行代碼）
- **技術棧**：Python (Flask/FastAPI) + pypdf
- **範例**：

  ```python
  from pypdf import PdfReader, PdfWriter
  writer = PdfWriter()
  writer.add_page(reader.pages[0])
  writer.encrypt("userPassword", "ownerPassword")
  ```

- **狀態**：待實作，優先於其他加密方案

### ZIP 加密備選方案

- **理由**：若需要純前端或無後端環境
- **方案**：導出 PDF → 壓縮成加密 ZIP → 用戶下載解壓
- **優缺**：跨平台支持 vs. 多一個解壓步驟
- **狀態**：備選，暫不優先

### 其他功能
- [ ] 快捷鍵支援：新增常用操作的鍵盤快捷鍵（如 Delete 刪除文字框）
- [ ] 縮圖側欄寬度調整：允許用戶動態改變側欄寬度以更好地預覽

---

## ✅ 已完成

### UIUX 優化第一波（2026-04-04）

- [x] `cursor: not-allowed`：禁用按鈕游標正確顯示
- [x] 全域 Loading 遮罩：載入、合併、拆分、輸出操作加旋轉圓圈 + 狀態文字
- [x] 所有圖示按鈕加 `aria-label`（◀ ▶ ✕ 字距按鈕等）
- [x] 加 `:focus-visible` 樣式，鍵盤使用者可見焦點位置

### UIUX 優化第二波（2026-04-04）

- [x] 色彩對比度修正：次要文字統一改為 `#9ca3af`（調深，提升可讀性）
- [x] 浮動標示面板加收起（▲/▼）按鈕，避免遮擋 PDF 內容
- [x] 圖示統一（部分）：主工具列、橡皮擦、新增文字、刪除文字框換為 SVG
- [x] 屬性面板統一：`#txt-props` 樣式改為與 `#ann-float-panel` 一致
